# craft-nook
An art supply inventory manager designed for artists to organize their tools

## Building the project

This project uses the Gradle Wrapper to ensure the correct Gradle version is used.
To build the project, run:

```bash
./gradlew build
```

Note: Do not use the `gradle` command from your system, as it may be a newer version incompatible with the project's dependencies.
